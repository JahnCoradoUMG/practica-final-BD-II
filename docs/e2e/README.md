# Evidencias E2E (SCRUM-23)

Los archivos `scenario-XX-latest.json` se generan al ejecutar:

```powershell
.\scripts\run-e2e-tests.ps1
```

Cada archivo contiene timestamp, payloads de respuesta API y resultado `PASS` / detalle del fallo.

No incluir secretos ni contraseñas en evidencias commitadas.
